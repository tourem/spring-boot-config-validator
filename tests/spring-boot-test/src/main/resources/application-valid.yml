# Scenario: All properties present (valid configuration)
# Expected: config-preflight should NOT report any errors

database:
  url: jdbc:postgresql://localhost:5432/testdb
  username: testuser
  password: secret123
  max-connections: 10
  timeout: 30000

api:
  endpoint: https://api.example.com
  api-key: test-api-key-12345
  retry-count: 3
  enable-cache: true
  cache-directory: /tmp/cache

messaging:
  broker-url: amqp://localhost:5672
  queue-name: test-queue
  username: guest
  password: guest
  connection-timeout: 5000
  auto-reconnect: true

spring:
  application:
    name: spring-boot-test-valid
server:
  port: 8080
