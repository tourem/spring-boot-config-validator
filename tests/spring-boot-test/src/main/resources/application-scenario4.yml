# Scenario 4: Multiple missing properties across all configs
# Expected: config-preflight should detect 6 missing properties

database:
  url: jdbc:postgresql://localhost:5432/testdb
  username: testuser
  # password: MISSING - should be detected
  max-connections: 10
  # timeout: MISSING - should be detected

api:
  # endpoint: MISSING - should be detected
  api-key: test-api-key-12345
  retry-count: 3
  enable-cache: true
  # cache-directory: MISSING - should be detected

messaging:
  broker-url: amqp://localhost:5672
  # queue-name: MISSING - should be detected
  username: guest
  password: guest
  # connection-timeout: MISSING - should be detected
  auto-reconnect: true

spring:
  application:
    name: spring-boot-test-scenario4
server:
  port: 8080
