# Configuration with multiple missing properties for testing config-preflight

database:
  url: jdbc:postgresql://localhost:5432/testdb
  username: testuser
  # password: missing - should trigger validation error
  max-connections: 10
  # timeout: missing - should trigger validation error

api:
  # endpoint: missing - should trigger validation error
  api-key: test-api-key-12345
  retry-count: 3
  enable-cache: true
  # cache-directory: missing - should trigger validation error

messaging:
  broker-url: amqp://localhost:5672
  # queue-name: missing - should trigger validation error
  username: guest
  password: guest
  # connection-timeout: missing - should trigger validation error
  auto-reconnect: true

micronaut:
  server:
    port: 8082
  application:
    name: micronaut-test
