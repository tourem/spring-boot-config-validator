# Scenario 2: Missing api.endpoint and api.cacheDirectory
# Expected: config-preflight should detect 2 missing properties

database:
  url: jdbc:postgresql://localhost:5432/testdb
  username: testuser
  password: secret123
  maxConnections: 10
  timeout: 30000

api:
  # endpoint: MISSING - should be detected
  apiKey: test-api-key-12345
  retryCount: 3
  enableCache: true
  # cacheDirectory: MISSING - should be detected

messaging:
  brokerUrl: amqp://localhost:5672
  queueName: test-queue
  username: guest
  password: guest
  connectionTimeout: 5000
  autoReconnect: true

micronaut:
  application:
    name: micronaut-test-scenario2
